#include <iostream>

#include "database.h"
#include "search.h"
#include "recipe.h"

using namespace std;

int main()
{
	database::initialize();
	while (true)
	{
		string input;
		getline(cin, input);
		
	}
	return 0;
}